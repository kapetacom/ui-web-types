/* SHAPE TYPES */

export interface Dimensions {
    top: number,
    left: number,
    width: number,
    height: number,
}

export interface Point {
    x:number
    y:number
}

export interface Size {
    width: number,
    height: number,
}

/* SHAPE HELPERS */